test.todo(
  'initial flight type is one-way and date fields have the same valid date value',
)

describe('one-way flight type', () => {
  test.todo('return date field is disabled')

  test.todo(
    'when start date field has invalid date, "Book" button is disabled and field is marked invalid',
  )

  test.todo('when user books a one-way flight, success message is displayed')
})

describe('return flight type', () => {
  test.todo('when return date is before start date, "Book" button is disabled')

  test.todo(
    'when date field has invalid date, "Book" button is disabled and field is marked invalid',
  )

  test.todo('when user books a return flight, success message is displayed')
})
